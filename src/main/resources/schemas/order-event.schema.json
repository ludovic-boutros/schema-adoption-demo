{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OrderEvent",
  "type": "object",
  "properties": {
    "orderId": {
      "type": "string",
      "description": "Unique identifier of the order"
    },
    "customerId": {
      "type": "string",
      "description": "Identifier of the customer who placed the order"
    },
    "amount": {
      "type": "number",
      "description": "Order total. Attempted change: was 'string' (e.g. \"19.99\"), now 'number' - the same retype that broke branch 02's consumer. This is what Schema Registry rejects."
    },
    "status": {
      "type": "string",
      "description": "Current order status, e.g. NEW or PAID"
    }
  },
  "required": ["orderId", "customerId", "amount", "status"],
  "additionalProperties": true
}
